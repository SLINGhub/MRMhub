#' @param autoscale Logical. When `TRUE` (default), `font_base_size` and
#'   `point_size` left as `NULL` are sized automatically from `cols_page` (more
#'   facet columns per page give smaller text and points). Any value passed
#'   explicitly always takes precedence. When `FALSE`, unset sizes fall back to
#'   the single-plot defaults.
