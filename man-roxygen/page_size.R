#' @param page_width,page_height Size of a PDF page, in `page_units`. Both must
#'   be given together. `NULL` (default) uses an A4 page of 280 x 200 mm,
#'   oriented by `page_orientation`. When an explicit size is given,
#'   `page_orientation` has no effect.
#' @param page_units Unit of `page_width` and `page_height`: `"mm"` (default),
#'   `"cm"`, `"in"` or `"pt"`.
