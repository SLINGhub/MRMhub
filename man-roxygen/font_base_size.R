#' @param font_base_size Numeric. Base font size (in points) for plot text; all
#'   plot text scales proportionally with this value. `NULL` (default) uses the
#'   global default set by [mrmhub_set_plot_defaults()] if one is in effect,
#'   otherwise an automatic size (derived from the facet-column count on paged
#'   plots, or the per-plot default shown in the Usage section above).
