#' @param legend_position Optional legend placement. One of `"right"`,
#'   `"left"`, `"top"`, `"bottom"`, `"none"`; a corner keyword `"inside-tr"`,
#'   `"inside-tl"`, `"inside-br"`, `"inside-bl"`; or a numeric `c(x, y)` in
#'   `[0, 1]` coordinates. `NULL` (default) keeps the current placement.
#' @param legend_size Optional single multiplier of `font_base_size` (when
#'   `<= 3`) or absolute point size (when `> 3`) that scales the whole legend:
#'   text, title, key and the plotted symbols. `NULL` (default) leaves the
#'   legend unchanged.
#' @param show_legend_title Logical. `TRUE` (default) keeps the legend title;
#'   `FALSE` hides it.
