% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct-batch.R
\name{correct_batch_combat}
\alias{correct_batch_combat}
\title{ComBat batch correction}
\usage{
correct_batch_combat(
  data = NULL,
  variable,
  ref_qc_types,
  covariates = NULL,
  ref_batch = NULL,
  parametric = TRUE,
  ignore_istd = TRUE,
  replace_previous = TRUE,
  log_transform_internal = TRUE,
  feature_list = NULL,
  replace_exisiting_trendcurves = FALSE
)
}
\arguments{
\item{data}{A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object.}

\item{variable}{The variable to correct: one of "intensity", "norm_intensity",
or "conc".}

\item{ref_qc_types}{Character vector of QC types used for the QC-CV report and
trend curves (not for the ComBat fit itself).}

\item{covariates}{Optional model matrix of biological covariates to preserve
(passed to \code{\link[sva:ComBat]{sva::ComBat()}} as \code{mod}). Defaults to \code{NULL} (no covariates).}

\item{ref_batch}{Optional reference batch to adjust the others towards
(passed to \code{\link[sva:ComBat]{sva::ComBat()}} as \code{ref.batch}). Defaults to \code{NULL}.}

\item{parametric}{Use the parametric empirical-Bayes prior (\code{TRUE}, default)
or the non-parametric prior (\code{FALSE}).}

\item{ignore_istd}{Logical. Exclude internal standards (ISTDs) from correction
if \code{TRUE} (the default). Their values are left unchanged, and they do not
contribute to the empirical-Bayes prior.}

\item{replace_previous}{Replace a previous batch correction (\code{TRUE}, default)
or apply on top of it.}

\item{log_transform_internal}{Fit ComBat in log10 space (\code{TRUE}, default,
appropriate for multiplicatively-scaling MS data). Returned data are always
back-transformed to the raw scale.}

\item{feature_list}{Optional feature selection (character vector or a single
regular expression); \code{NULL} (default) selects all features.}

\item{replace_exisiting_trendcurves}{Reseed the plotting trend curves. Default
\code{FALSE}.}
}
\value{
A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} with corrected data.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Adjusts batch effects with the empirical-Bayes ComBat method (Johnson et al.
2007), applied to one of "intensity", "norm_intensity", or "conc". ComBat
models a location and scale batch effect per feature and shrinks those
estimates across features, which can stabilise many small batches better than
simple median centering.

Unlike \code{\link[=correct_batch_centering]{correct_batch_centering()}} and \code{\link[=correct_batch_serrf]{correct_batch_serrf()}}, ComBat
estimates batch effects from \strong{all} samples (optionally protecting biology
via \code{covariates}), not from the reference QCs. On strongly unbalanced designs
this can remove genuine biological signal, so supply \code{covariates} when the
biological grouping is not balanced across batches. \code{ref_qc_types} is used
only for the before/after QC-CV report and the plotting trend curves.

Batch correction is performed \strong{after} normalization and drift correction in
the recommended pipeline. Features with any missing or non-finite values in
the selected variable are left uncorrected (ComBat requires complete data).
}
\examples{
\dontshow{if (rlang::is_installed("sva")) withAutoprint(\{ # examplesIf}
# mexp <- correct_batch_combat(mexp, variable = "conc", ref_qc_types = "BQC")
\dontshow{\}) # examplesIf}
}
\references{
Johnson WE, Li C, Rabinovic A (2007). Adjusting batch effects in microarray
expression data using empirical Bayes methods. \emph{Biostatistics}, 8(1),
118-127. \doi{10.1093/biostatistics/kxj037}

Applied through \code{\link[sva:ComBat]{sva::ComBat()}} from the \code{sva} package. See also
Broadhurst D, et al. (2018), \emph{Metabolomics}, 14, 72
(\doi{10.1007/s11306-018-1367-3}) on QC-based signal correction.
}
\seealso{
\code{\link[=correct_batch_centering]{correct_batch_centering()}}, \code{\link[=correct_batch_serrf]{correct_batch_serrf()}},
\code{\link[=correct_drift_loess]{correct_drift_loess()}} and \code{\link[=plot_runscatter]{plot_runscatter()}} for visualisation. The
\href{https://slinghub.github.io/MRMhub/quant/articles/manual-07-corrections.html}{drift and batch correction manual}.
}
