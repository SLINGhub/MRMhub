% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions-math.R
\name{pooled_rsd}
\alias{pooled_rsd}
\title{Pooled percent relative standard deviation (\%RSD) across groups}
\usage{
pooled_rsd(x, group, na.rm = FALSE, min_n = 2L)
}
\arguments{
\item{x}{a numeric vector}

\item{group}{a vector the same length as \code{x} giving the pooling group of each
value (e.g. \code{batch_id})}

\item{na.rm}{logical, if \code{TRUE} then NA values in \code{x} are stripped before
pooling}

\item{min_n}{integer, the minimum number of non-missing values a group must
have to contribute. Groups with fewer than \code{min_n} are dropped; a group is
always dropped below two values, which have no within-group variance. The
default is \code{2}.}
}
\value{
a single numeric value. \code{NA_real_} if the grand mean is zero or NA,
\code{x} is not numeric, or no group has enough replicates.
}
\description{
\code{\link[=pooled_sd]{pooled_sd()}} expressed as a percentage of the grand mean of \code{x}, i.e. a
batch-pooled \%CV. The pooled SD weights each group by its degrees of freedom
and the grand mean weights each value equally, so both are consistently
weighted by group size. See \code{\link[=pooled_sd]{pooled_sd()}} for how groups are pooled.
}
\examples{
pooled_rsd(c(5, 6, 4, 15, 16, 14), group = c(1, 1, 1, 2, 2, 2))
}
