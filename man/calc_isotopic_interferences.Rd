% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isotope-derive.R
\name{calc_isotopic_interferences}
\alias{calc_isotopic_interferences}
\title{Derive isotopic interference relationships}
\usage{
calc_isotopic_interferences(
  data = NULL,
  level = c("MRM", "MS1"),
  mz_tol = 0.5,
  min_contribution = 0,
  check_coelution = FALSE
)
}
\arguments{
\item{data}{A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object.}

\item{level}{Correction level, \code{"MRM"} or \code{"MS1"}. See description.}

\item{mz_tol}{Precursor m/z tolerance (Da) for matching interfering pairs at
the MS1 level (MRM uses a fixed 2 +/- 0.2 Da precursor window). Default \code{0.5}.}

\item{min_contribution}{Drop derived edges whose contribution factor \code{K} is
below this value. Default \code{0} (keep all). Raise to skip negligible or
borderline pairs (e.g. \code{0.002}).}

\item{check_coelution}{\emph{(experimental)} If \code{TRUE}, an m/z-matched edge is kept
only if the interferer and victim co-elute -- the interferer's peak apex
falls within the victim's integration window (imported borders
\code{feature_int_start}/\code{feature_int_end}, else \verb{feature_rt +/- FWHM}).
Chromatographically resolved pairs are dropped. Default \code{FALSE} while the
gate is validated.}
}
\value{
The \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} with a populated \code{annot_interferences} slot.
}
\description{
Automatically discovers isotopic (M+2) interference relationships
between measured features and stores them in the \code{annot_interferences} slot,
ready for \code{\link[=correct_isotopic_interferences]{correct_isotopic_interferences()}}. Two levels are supported:
\itemize{
\item \strong{\code{"MRM"}} (default): fragment-based front/back correction for class-based
LC-MRM data, derived from the LICAR method (see the manual). Requires
precursor \strong{and} product m/z and an \code{mrm_pattern} per feature.
\item \strong{\code{"MS1"}}: whole-molecule M+2 correction for \strong{MS1 / full-scan}
acquisitions. The factor is the M+2 relative abundance of each species'
molecular formula; interfering pairs are matched by precursor m/z (~2 Da
apart) within a feature class. Requires precursor m/z only.
}
}
\details{
\strong{Important — MS1 is not a fallback for MRM.} For MRM data the
isotopic correction must be \emph{fragment-based}: the contribution of heavy
isotopes to a transition depends on the isotope's location relative to the
fragmentation (Gao et al. 2021). The whole-molecule (\code{"MS1"}) level is only
valid for genuine MS1/full-scan measurements and must \strong{not} be used as a
substitute for MRM data that happens to lack a product m/z.

Derived factors are sensitive to the \pkg{enviPat} version; version 2.8 is the
reference. Existing manual interferences (\code{source == "manual"}) are preserved;
previously derived (\code{"auto"}) rows are replaced.
\subsection{How the contribution factor is computed}{

The contribution \code{K} is the fraction of a lighter species' signal that appears
at M+2; it is subtracted downstream by \code{\link[=correct_isotopic_interferences]{correct_isotopic_interferences()}} as
\eqn{value_{corrected} = value_{raw} - K \cdot value_{interferer}}.
\itemize{
\item \strong{MS1 (whole-molecule):} \eqn{K = \sum ab(\text{peaks within } \pm 0.5
  \text{ Da of } M_0 + 2)\, /\, ab(M_0)}, computed from the species' molecular
formula with \pkg{enviPat} \code{isopattern} (see \code{mN_rel_abundance()}).
\item \strong{MRM (fragment-based, LICAR):} the fragment elemental formula is built from
the precursor carbon/hydrogen count plus a per-class offset \code{c(C, H, O, N)},
with \eqn{H = 2C + c_H - 2 H_{raw}}; \code{K} is that fragment's M+2 isotopologue
fraction from \pkg{enviPat} \code{isowrap} (centroided, algorithm 2 — chosen over
\code{isopattern} for parity with published LICAR values). \emph{Front} vs \emph{back}
distinguishes whether the heavy isotope lands on the retained product ion or
the neutral loss; FA/LCB species carry a front + back pair, and LCB inverts
the front/back assignment relative to FA.
}
}
}
\references{
Gao L. et al. (2021). LICAR: An Application for Isotopic Correction
of Targeted Lipidomic Data Acquired with Class-Based Chromatographic
Separations Using Multiple Reaction Monitoring. \emph{Analytical Chemistry},
93(6), 3163-3171. \doi{10.1021/acs.analchem.0c04565}
}
