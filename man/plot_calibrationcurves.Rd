% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-calibcurves.R
\name{plot_calibrationcurves}
\alias{plot_calibrationcurves}
\title{Plot calibration curves}
\usage{
plot_calibrationcurves(
  data = NULL,
  variable = "norm_intensity",
  qc_types = NA,
  fit_overwrite,
  fit_model = c("linear", "quadratic"),
  fit_weighting = c(NA, "none", "1/x", "1/x^2"),
  ci_show = NA,
  ci_clip = TRUE,
  zoom_n_points = NA,
  log_scale = FALSE,
  filter_data = FALSE,
  include_qualifier = TRUE,
  include_istd = FALSE,
  include_feature_filter = NA,
  exclude_feature_filter = NA,
  output_pdf = FALSE,
  path = NA,
  create_dir = TRUE,
  return_plots = FALSE,
  point_size = NULL,
  point_color = NA,
  point_fill = NA,
  point_shape = NA,
  line_width = 0.7,
  line_color = "#4575b4",
  ribbon_fill = "#e6f6ff",
  font_base_size = NULL,
  rows_page = 4,
  cols_page = 5,
  specific_page = NA,
  page_orientation = "LANDSCAPE",
  page_width = NULL,
  page_height = NULL,
  page_units = "mm",
  show_progress = TRUE
)
}
\arguments{
\item{data}{A \code{MRMhubExperiment} object.}

\item{variable}{Variable to plot on the y-axis, usually intensity. Default
is \code{"intensity"}.}

\item{qc_types}{A character vector specifying the QC types to plot. It must contain
at least \code{CAL}, which represents calibration curve samples. Other QC types will be
plotted as points when they have assigned concentrations (see QC-concentration metadata).
These QC types need to be present in the data and defined in the analysis metadata.
The default is \code{NA}, which means any of the QC types "CAL", "HQC", "MQC", "LQC",
"EQA", "QC", will be plotted if present and have assigned concentrations.}

\item{fit_overwrite}{If \code{TRUE},
the function will use the provided \code{fit_model} and \code{fit_weighting} values
for all analytes and ignore any fit method and weighting settings defined in
the metadata.}

\item{fit_model}{A character string specifying the default regression fit
method to use for the calibration curve. Must be one of \code{"linear"} or
\code{"quadratic"}. This method will be applied if no specific fit method is
defined for a feature in the metadata, or
when \code{fit_overwrite = TRUE}.}

\item{fit_weighting}{A character string specifying the default weighting
method for the regression points in the calibration curve. Must be one of
\code{"none"}, \code{"1/x"}, or \code{"1/x^2"}. This method will be applied if no
specific weighting method is defined for a feature in the metadata, or
when \code{fit_overwrite = TRUE}.}

\item{ci_show}{Logical, if \code{TRUE}, displays the confidence interval as ribbon.
Default is \code{NA}, in which case confidence intervals are plotted in a linear
scale and omitted in log-log scale.}

\item{ci_clip}{Logical, if \code{TRUE}, clips the confidence interval above or below the highest and lowest data point, respectively.}

\item{zoom_n_points}{Number of x lowest concentration points to display, used for zooming. Set to \code{NULL} or \code{NA} (default) to show all points.}

\item{log_scale}{Logical. Determines whether the x and y axes are displayed in a logarithmic scale (log-log scale).
Set to \code{TRUE} to enable logarithmic scaling; otherwise, set to \code{FALSE} for a linear scale.
Note: If \code{TRUE}, any regression curves or standard error regions with negative
values will be omitted from display.}

\item{filter_data}{Logical, if \code{TRUE}, uses QC filtered data; otherwise uses
raw data. Default is \code{FALSE}.}

\item{include_qualifier}{Logical, whether to include qualifier features. Default is \code{TRUE}.}

\item{include_istd}{Logical, whether to include internal standard (ISTD) features. Default is \code{TRUE}.}

\item{include_feature_filter}{Feature(s) to include by \code{feature_id}, as a
character vector. Each element is matched exactly when it names an existing
feature, otherwise treated as a regex; elements combine with OR. A full ID
(e.g. \code{"S1P d18:0 [M>60]"}) needs no escaping, while patterns like \code{"PC|PE"}
still work. \code{NA} or \code{""} ignores the filter.}

\item{exclude_feature_filter}{Feature(s) to exclude by \code{feature_id}, matched
the same way as \code{include_feature_filter}. \code{NA} or \code{""} ignores the filter.}

\item{output_pdf}{Logical, if \code{TRUE}, saves plots as a PDF file. Default is
\code{FALSE}.}

\item{path}{File path for saving the PDF. Default is an empty string.}

\item{create_dir}{A logical value. If \code{TRUE} (the default) and \code{output_pdf}
is \code{TRUE}, the parent directory of \code{path} is created if it does not yet exist.}

\item{return_plots}{Logical, if \code{TRUE}, returns plots as a list of \code{ggplot}
objects. Default is \code{FALSE}.}

\item{point_size}{Size of points in the plot. Default is 1.5.}

\item{point_color}{A vector specifying the colors for points corresponding
to different QC types. This can be either an unnamed vector or a named
vector, with names corresponding to QC types. Unused colors will be ignored.
Default is \code{NA} which corresponds to the default colors for QC types defined in the package.}

\item{point_fill}{A vector specifying the fill colors for points corresponding
to different QC types. This can be either an unnamed vector or a named
vector, with names corresponding to QC types. Unused fill colors will be ignored.
Default is \code{NA} which corresponds to the default fill colors for QC types defined in the package.}

\item{point_shape}{A vector specifying the shapes for points corresponding
to different QC types. This can be either an unnamed vector or a named
vector, with names corresponding to QC types. Unused shapes will be ignored.
Default is \code{NA} which corresponds to the default shapes for QC types defined in the package.}

\item{line_width}{Width of regression lines. Default is 0.7.}

\item{line_color}{Color of the regression line. Default is \code{"#4575b4"}.}

\item{ribbon_fill}{Color for the confidence interval ribbon. Default is
\code{"#91bfdb40"}.}

\item{font_base_size}{Numeric. Base font size (in points) for plot text; all
plot text scales proportionally with this value. \code{NULL} (default) uses the
global default set by \code{\link[=mrmhub_set_plot_defaults]{mrmhub_set_plot_defaults()}} if one is in effect,
otherwise an automatic size (derived from the facet-column count on paged
plots, or the per-plot default shown in the Usage section above).}

\item{rows_page}{Number of plot rows. Default is 4.}

\item{cols_page}{Number of plot columns. Default is 5.}

\item{specific_page}{Show/save a specific page number only. \code{NA} plots/saves all pages.}

\item{page_orientation}{Orientation of PDF, either \code{"LANDSCAPE"} or
\code{"PORTRAIT"}. Default is \code{"LANDSCAPE"}. Ignored when \code{page_width} and
\code{page_height} are given.}

\item{page_width, page_height}{Size of a PDF page, in \code{page_units}. Both must
be given together. \code{NULL} (default) uses an A4 page of 280 x 200 mm,
oriented by \code{page_orientation}. When an explicit size is given,
\code{page_orientation} has no effect.}

\item{page_units}{Unit of \code{page_width} and \code{page_height}: \code{"mm"} (default),
\code{"cm"}, \code{"in"} or \code{"pt"}.}

\item{show_progress}{Logical. If \code{TRUE}, displays a progress bar during
plot creation.}
}
\value{
A list of \code{ggplot} objects if \code{return_plots = TRUE}, otherwise
\code{NULL} (the plots are drawn to the active device or written to a PDF).
}
\description{
This function plots calibration curves of each feature where defined
and displays QC samples with defined concentrations within the plot.
Users can select a regression model (\code{linear} or \code{quadratic}) and apply
weighting (\code{none}, \code{"1/x"}, or \code{"1/x^2"}), either through function arguments
or feature metadata.
}
\details{
Features for plotting can be filtered using QC filters defined via
\code{\link[=filter_features_qc]{filter_features_qc()}} or through \code{include_feature_filter} and
\code{exclude_feature_filter} arguments. The resulting plots offer extensive
customization options, including point size, line width, point color, point
fill, point shape, line color, ribbon fill, and font base size.

Plots will be divided into multiple pages if the number of features exceeds
the product of \code{rows_page} and \code{cols_page} settings. The function supports
both direct plotting within R and saving plots as PDF files. Additionally,
plots can be returned as a list of ggplot2 objects for further manipulation
or integration into other analyses.
}
\section{Preferred formats and devices}{
\tabular{llll}{
   Purpose \tab Format \tab Device used \tab Typical \code{dpi} \cr
   Journal figure, vector (default choice) \tab \code{"pdf"} \tab \code{grDevices::cairo_pdf}, else \code{grDevices::pdf} \tab n/a \cr
   Figure for further editing (Illustrator, Inkscape) \tab \code{"svg"} \tab \code{svglite::svglite}, else \code{grDevices::svg} \tab n/a \cr
   Slides, Quarto HTML, GitHub \tab \code{"png"} \tab \code{ragg::agg_png}, else \code{grDevices::png} \tab 150-300 \cr
   Journal requiring raster submission \tab \code{"tiff"} \tab \code{ragg::agg_tiff}, else \code{grDevices::tiff} \tab 300-600 \cr
}


Prefer a \strong{vector} format (\code{pdf}, \code{svg}) for publication: text stays
selectable and searchable, and lines stay sharp at any magnification.

Prefer a \strong{raster} format (\code{png}, \code{tiff}) when a plot draws very many marks
-- a \code{\link[=plot_runscatter]{plot_runscatter()}} page covering several thousand analyses, or a dense
\code{\link[=plot_pca]{plot_pca()}} score plot. Every point becomes a separate object in a PDF, so
such figures produce very large files that are slow to open and to typeset.
Saving them at 300-600 dpi instead keeps the file small with no visible loss.

The optional packages \code{ragg} and \code{svglite} are used automatically when
installed, giving better text rendering, system-font support and smaller SVG
files. When they are absent the equivalent \code{grDevices} device is used and the
output is still correct. Installing both is recommended:
\code{install.packages(c("ragg", "svglite"))}.

PDF output uses the cairo device wherever R was built with cairo support
(\code{capabilities("cairo")}), because plain \code{grDevices::pdf()} writes text in a
single-byte encoding and silently transliterates anything outside it -- an en
dash becomes \code{-}, \code{>=} replaces the proper symbol. Unit labels such as
\code{umol/L} and statistical annotations routinely depend on those glyphs.

Multi-page output from the paged plot functions (\code{\link[=plot_runscatter]{plot_runscatter()}},
\code{\link[=plot_calibrationcurves]{plot_calibrationcurves()}}, \code{\link[=plot_responsecurves]{plot_responsecurves()}},
\code{\link[=plot_feature_correlations]{plot_feature_correlations()}}) is PDF only, which is the only format that
holds many pages in one file. Use \code{\link[=save_plot]{save_plot()}} for single figures in any of
the other formats.
}

\seealso{
\code{\link[=save_plot]{save_plot()}} to save a single figure in any format.

Other calibration plots:
\code{\link[=plot_responsecurves]{plot_responsecurves()}}
}
\concept{calibration plots}
