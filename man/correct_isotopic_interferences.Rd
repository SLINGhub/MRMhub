% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct-isotope.R
\name{correct_isotopic_interferences}
\alias{correct_isotopic_interferences}
\title{Correct automatically derived isotopic interferences}
\usage{
correct_isotopic_interferences(
  data = NULL,
  variable = "feature_intensity",
  sequential_correction = TRUE,
  neg_to_na = FALSE
)
}
\arguments{
\item{data}{MRMhubExperiment object.}

\item{variable}{Name of the variable to correct. Only \code{"feature_intensity"}
(the raw intensity) is supported. Default: \code{"feature_intensity"}.}

\item{sequential_correction}{Logical. If \code{TRUE} (default), a chain of
interferences is corrected sequentially so each feature uses the
already-corrected signal of its interferer (propagates along the chain). If
\code{FALSE}, each feature is corrected from the raw interferer, without
propagation.}

\item{neg_to_na}{If \code{TRUE}, negative or zero values after correction are
replaced with \code{NA}. Default: \code{FALSE}.}
}
\value{
MRMhubExperiment object with feature intensities corrected.
}
\description{
Applies the isotopic (M+2) interference corrections previously
discovered by \code{\link[=calc_isotopic_interferences]{calc_isotopic_interferences()}} (and any declared interferences,
see \code{\link[=correct_custom_interferences]{correct_custom_interferences()}}) to the raw feature intensities. Aborts
with guidance if no interferences have been derived yet.

The subtraction is \deqn{value_{corrected} = value_{raw} - K \cdot
value_{interferer}} applied on the raw \code{feature_intensity}. For auto-derived
(\code{source == "auto"}) edges the interferer is clamped at 0 before subtraction
and the result clamped at 0 (LICAR parity); declared (\code{"manual"}) edges are
unclamped. See \code{\link[=calc_isotopic_interferences]{calc_isotopic_interferences()}} for how \code{K} is computed.

A chain (e.g. PC 34:2 > PC 34:1 > PC 34:0) is corrected sequentially by
default, each feature using its already-corrected interferer; set
\code{sequential_correction = FALSE} to correct each from the raw interferer. The
raw signal is preserved in \code{feature_intensity_orig}, and the correction is
idempotent (re-running restores from raw first).
}
\references{
Gao L., Ji S, Burla B, Wenk MR, Torta F, & Cazenave-Gassiot A
(2021). LICAR: An Application for Isotopic Correction of Targeted Lipidomic
Data Acquired with Class-Based Chromatographic Separations Using Multiple
Reaction Monitoring. \emph{Analytical Chemistry}, 93(6), 3163-3171.
\url{https://doi.org/10.1021/acs.analchem.0c04565}
}
