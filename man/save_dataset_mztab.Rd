% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-mztab.R
\name{save_dataset_mztab}
\alias{save_dataset_mztab}
\title{Export an experiment to mzTab-M (HUPO-PSI)}
\usage{
save_dataset_mztab(
  data = NULL,
  path,
  variable = "conc",
  instrument = NULL,
  contact = NULL,
  publication = NULL,
  rt_in_minutes = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{data}{An \code{MRMhubExperiment} object.}

\item{path}{Output file path. A \code{.mzTab} extension is appended if missing.}

\item{variable}{Feature variable used as abundance. One of \code{"conc"},
\code{"intensity"}, \code{"norm_intensity"}, \code{"area"} or \code{"height"}. Default
\code{"conc"} (falls back to \code{"intensity"} if not quantified).}

\item{instrument, contact, publication}{Optional single strings used to
enrich the \code{MTD} metadata header (instrument name, contact name,
publication identifier). \code{NULL} (default) omits them.}

\item{rt_in_minutes}{Logical; are \code{feature_rt} values in minutes? mzTab-M
stores retention time in seconds, so values are multiplied by 60 when
\code{TRUE} (default).}

\item{overwrite}{Logical; overwrite an existing file. Default \code{TRUE}.}
}
\value{
Invisibly returns the (normalised) output \code{path}. Called for its
side effect of writing the file.
}
\description{
Writes an \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} to an
\href{https://github.com/HUPO-PSI/mzTab-M}{mzTab-M 2.0.0-M} file, the HUPO-PSI
community standard for reporting quantitative metabolomics / lipidomics
results (and the format expected by repositories such as MetaboLights).
}
\details{
The full dataset is exported: every analysis becomes an \code{assay} (QC,
blank and calibration samples included), every feature a Small Molecule
Feature (\code{SMF}) row, and features are grouped by analyte into Small Molecule
Summary (\code{SML}) rows. A minimal Small Molecule Evidence (\code{SME}) row is
emitted per feature so all three table types are present.

\strong{Abundance.} \code{variable} selects which \verb{feature_*} value is written to the
\code{abundance_assay[n]} columns. The default \code{"conc"} uses final concentrations
and declares the matching concentration unit; if the experiment has not been
quantified, the exporter falls back to raw \code{feature_intensity} with an
"Arbitrary quantification unit".

\strong{What is \emph{not} exported.} mzTab-M is a quantification report, not a
processing-state container. Internal-standard relationships, QC/calibration
metrics, drift/batch-correction state and the \code{qc_type}/batch structure are
not representable and are therefore not written (ISTD features are merely
flagged via an \code{opt_global_is_internal_standard} column). See the
\emph{mzTab-M export} recipe article for the full mapping.
}
\examples{
\dontrun{
save_dataset_mztab(mexp, "experiment.mzTab")
save_dataset_mztab(mexp, "raw.mzTab", variable = "area")
}

}
\seealso{
\code{\link[=save_report_xlsx]{save_report_xlsx()}}, \code{\link[=save_dataset_csv]{save_dataset_csv()}}
}
