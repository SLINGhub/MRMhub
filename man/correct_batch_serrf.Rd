% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct-batch.R
\name{correct_batch_serrf}
\alias{correct_batch_serrf}
\title{SERRF batch correction}
\usage{
correct_batch_serrf(
  data = NULL,
  variable,
  ref_qc_types,
  n_correlated = 10,
  num_trees = 500,
  seed = 1L,
  num_threads = 1L,
  show_progress = TRUE,
  replace_previous = TRUE,
  feature_list = NULL,
  replace_exisiting_trendcurves = FALSE
)
}
\arguments{
\item{data}{A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object.}

\item{variable}{The variable to correct: one of "intensity", "norm_intensity",
or "conc".}

\item{ref_qc_types}{Character vector of QC types used as the SERRF training
(reference) samples per batch.}

\item{n_correlated}{Number of most-correlated features used as random-forest
predictors per feature. Default \code{10}.}

\item{num_trees}{Number of trees per random forest. Default \code{500}.}

\item{seed}{Random seed for \code{\link[ranger:ranger]{ranger::ranger()}}, for reproducibility.
Default \code{1}.}

\item{num_threads}{Threads per random forest passed to \code{\link[ranger:ranger]{ranger::ranger()}}.
Default \code{1}; kept low because batches are already corrected in parallel and
each forest trains on a small QC set.}

\item{show_progress}{Show a progress bar over batches. Default \code{TRUE}.}

\item{replace_previous}{Replace a previous batch correction (\code{TRUE}, default)
or apply on top of it.}

\item{feature_list}{Optional feature selection (character vector or a single
regular expression); \code{NULL} (default) selects all features.}

\item{replace_exisiting_trendcurves}{Reseed the plotting trend curves. Default
\code{FALSE}.}
}
\value{
A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} with corrected data.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

This is an independent re-implementation of SERRF in \code{mrmhub}, adapted from the
reference code in the \code{malbacR} package (not the original authors' package);
see Details. Validate results against the reference SERRF for your data.

Normalises systematic error with SERRF (Systematic Error Removal using Random
Forest; Fan et al. 2019). For each feature and batch a random forest is
trained on the reference QC samples, using the batch's most-correlated
features as predictors, and the learned systematic error is removed from all
samples. Unlike ComBat, SERRF captures non-linear drift and batch effects
jointly and is anchored on the QC samples, matching the QC-based design of the
package; it is best suited to larger panels with dense QC coverage.

SERRF operates on the raw abundance scale (no log transform). Features with
missing or non-positive values, and batches with fewer than two reference
QCs, are left uncorrected. Batch correction is performed \strong{after}
normalization and drift correction in the recommended pipeline.
}
\details{
The implementation adapts the reference SERRF code in the \code{malbacR} package,
keeping its normalization arithmetic but selecting each feature's predictors
from the per-batch QC-sample Spearman correlation (a simplification of
malbacR's QC/sample correlation intersection). Random forests are fit with
\code{\link[ranger:ranger]{ranger::ranger()}} and a fixed \code{seed}, so results are reproducible. Users are
encouraged to validate results against the reference SERRF implementation for
their data.

Batches are corrected independently and in parallel via \code{mirai} (through
\code{\link[purrr:in_parallel]{purrr::in_parallel()}}); set up workers with \code{\link[mirai:daemons]{mirai::daemons()}} beforehand to
use them, otherwise the batches are processed sequentially.
}
\examples{
\dontshow{if (rlang::is_installed("ranger")) withAutoprint(\{ # examplesIf}
# mexp <- correct_batch_serrf(mexp, variable = "conc", ref_qc_types = "BQC")
\dontshow{\}) # examplesIf}
}
\references{
Fan S, Kind T, Cajka T, et al. (2019). Systematic Error Removal Using Random
Forest for Normalizing Large-Scale Untargeted Lipidomics Data. \emph{Analytical
Chemistry}, 91(5), 3590-3596. \doi{10.1021/acs.analchem.8b05592}

Implementation adapted from the \code{malbacR} package
(\url{https://github.com/pmartR/malbacR}); random forests via \code{ranger}
(Wright MN, Ziegler A, 2017, \emph{Journal of Statistical Software}, 77(1),
\doi{10.18637/jss.v077.i01}).
}
\seealso{
\code{\link[=correct_batch_centering]{correct_batch_centering()}}, \code{\link[=correct_batch_combat]{correct_batch_combat()}},
\code{\link[=correct_drift_loess]{correct_drift_loess()}} and \code{\link[=plot_runscatter]{plot_runscatter()}} for visualisation. The
\href{https://slinghub.github.io/MRMhub/quant/articles/manual-07-corrections.html}{drift and batch correction manual}.
}
