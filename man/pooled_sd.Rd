% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions-math.R
\name{pooled_sd}
\alias{pooled_sd}
\title{Pooled standard deviation across groups}
\usage{
pooled_sd(x, group, na.rm = FALSE, min_n = 2L)
}
\arguments{
\item{x}{a numeric vector}

\item{group}{a vector the same length as \code{x} giving the pooling group of each
value (e.g. \code{batch_id})}

\item{na.rm}{logical, if \code{TRUE} then NA values in \code{x} are stripped before
pooling}

\item{min_n}{integer, the minimum number of non-missing values a group must
have to contribute. Groups with fewer than \code{min_n} are dropped; a group is
always dropped below two values, which have no within-group variance. The
default is \code{2}.}
}
\value{
a single numeric value. \code{NA_real_} if \code{x} is not numeric or no group
has enough replicates to pool.
}
\description{
Pools the within-group variances of \code{x} across the levels of \code{group},
weighting each group by its degrees of freedom (\code{n_i - 1}), and returns the
square root. This is the within-group precision estimate: unlike a plain
\code{sd(x)} it is not inflated by systematic shifts \emph{between} groups (e.g. batch
offsets or drift), so it is the appropriate spread for a batch-pooled \%RSD.
}
\examples{
pooled_sd(c(5, 6, 4, 15, 16, 14), group = c(1, 1, 1, 2, 2, 2))
}
