% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-workflow.R
\name{generate_workflow_qmd}
\alias{generate_workflow_qmd}
\title{Generate a runnable Quarto (.qmd) mrmhub workflow}
\usage{
generate_workflow_qmd(spec)
}
\arguments{
\item{spec}{A list describing the workflow. Recognised fields:
\describe{
\item{\code{importer}}{One of \code{"mrmhub"}, \code{"masshunter"}, \code{"skyline"},
\code{"csv_long"}, \code{"csv_wide"}. Default \code{"mrmhub"}.}
\item{\code{data_path}}{Path to the data file as it should appear in the
generated document.}
\item{\code{metadata_route}}{\code{"embedded"}, \code{"msorganiser"}, \code{"tables"}, or
\code{"none"}. Default \code{"embedded"}.}
\item{\code{metadata_path}}{Path to the metadata file (when not embedded).}
\item{\code{steps}}{Character vector of step ids to include (the ids defined
in \code{workflow_steps()}).}
\item{\code{drift_method}}{Drift model for the drift step: \code{"gaussian"}
(default), \code{"spline"}, or \code{"loess"}.}
\item{\code{ref_qc_types}, \code{reference_sample_id}}{Optional reference QC type(s)
and reference sample id for drift/batch/calibration. The corrected
feature variable is derived automatically as the highest processing
level reached by the selected steps.}
\item{\code{column_mapping}}{Named character vector for \code{importer = "csv_long"},
mapping canonical names to file columns, e.g.
\code{c(analysis_id = "Sample", feature_id = "Compound", feature_area = "Area")}.}
\item{\code{variable_name}, \code{analysis_id_col}, \code{first_feature_column}}{Arguments
for \code{importer = "csv_wide"}.}
\item{\code{output_xlsx}}{Path for the exported report. Default
\code{"results.xlsx"}.}
\item{\code{formats}}{Character vector of Quarto output formats, any of
\code{"html"}, \code{"docx"}, \code{"pdf"}. \code{"pdf"} is rendered sans-serif. Default
\code{"html"}.}
\item{\code{save_rds}}{Logical; also save the object as \code{.rds}. Default
\code{FALSE}.}
\item{\code{title}}{Document title. Default \code{"MRMhub Workflow"}.}
}}
}
\value{
A length-1 character string containing the \code{.qmd} source.
}
\description{
Assembles a self-contained Quarto document that imports data and metadata and
runs the selected processing steps, in canonical order, using the same
function calls and defaults as the package tutorials. This is a pure function
(no file I/O, no Shiny) so it can be tested and scripted directly; the
interactive \code{\link[=build_workflow]{build_workflow()}} app calls it to power its live preview.
}
\examples{
cat(generate_workflow_qmd(list(
  importer = "mrmhub",
  data_path = "MRMhub_demo.tsv",
  steps = c("normalize_istd", "quantify_istd")
)))
}
