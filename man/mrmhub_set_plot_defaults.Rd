% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-defaults.R
\name{mrmhub_set_plot_defaults}
\alias{mrmhub_set_plot_defaults}
\title{Set global default appearance for MRMhub plots}
\usage{
mrmhub_set_plot_defaults(
  font_base_size = NULL,
  point_size = NULL,
  legend_position = NULL,
  legend_size = NULL,
  show_legend_title = NULL,
  strip_bg_color = NULL,
  units = NULL,
  dpi = NULL
)
}
\arguments{
\item{font_base_size}{Base font size in points; all plot text scales from it.}

\item{point_size}{Point/marker size.}

\item{legend_position}{Legend placement: a keyword (\code{"right"}, \code{"left"},
\code{"top"}, \code{"bottom"}, \code{"none"}), a corner (\code{"inside-tr"}, \code{"inside-tl"},
\code{"inside-br"}, \code{"inside-bl"}), or a numeric \code{c(x, y)}.}

\item{legend_size}{Scales the whole legend (text, title, key and glyphs); a
value \verb{<= 3} is a multiplier of \code{font_base_size}, larger values are points.}

\item{show_legend_title}{\code{FALSE} hides legend titles.}

\item{strip_bg_color}{Facet strip background fill (strip text auto-contrasts).}

\item{units}{Default unit for the \code{width} and \code{height} of \code{\link[=save_plot]{save_plot()}}:
\code{"mm"} (the built-in default), \code{"cm"}, \code{"in"}, \code{"pt"} or \code{"px"}.}

\item{dpi}{Default resolution in dots per inch for the raster formats of
\code{\link[=save_plot]{save_plot()}}. The built-in default is \code{300}.}
}
\value{
Invisibly, a named list of the option values as they were before this
call (as returned by \code{\link[=options]{options()}}).
}
\description{
Sets session-wide defaults for the shared appearance arguments of the
\verb{plot_*()} functions, so common choices such as a smaller base font or point
size can be made once at the top of a notebook instead of on every call.
Defaults are stored as \verb{mrmhub.*} options. The precedence for every argument
is: a value passed explicitly to a plotting function wins, then the global
default set here, then the function's built-in default (including automatic
\code{cols_page}-based sizing on faceted plots).
}
\details{
Only the arguments you supply are changed; the rest are left untouched. Pass
\code{NULL} (the default) to leave a setting as it is, and use
\code{\link[=mrmhub_reset_plot_defaults]{mrmhub_reset_plot_defaults()}} to clear them all. The previous option values
are returned invisibly, so a set can be undone with \code{options(old)} or scoped
to a block with \code{\link[withr:local_options]{withr::local_options()}}.
}
\examples{
old <- mrmhub_set_plot_defaults(font_base_size = 8, point_size = 0.8)
# ... make plots with the smaller defaults ...
options(old) # restore
}
\seealso{
\code{\link[=mrmhub_reset_plot_defaults]{mrmhub_reset_plot_defaults()}}, \code{\link[=mrmhub_get_plot_defaults]{mrmhub_get_plot_defaults()}},
\code{\link[=save_plot]{save_plot()}} and the \code{vignette("manual-08-visualization")} article.
}
