% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-qc-interference.R
\name{plot_qc_interference_impact}
\alias{plot_qc_interference_impact}
\title{Plot the magnitude of interference correction as a histogram}
\usage{
plot_qc_interference_impact(
  data,
  qc_types = "SPL",
  include_qualifier = FALSE,
  include_istd = TRUE,
  include_feature_filter = NA,
  exclude_feature_filter = NA,
  min_correction_pct = NA,
  binwidth = NA,
  font_base_size = 8
)
}
\arguments{
\item{data}{A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} (already interference-corrected).}

\item{qc_types}{QC type(s) to summarize. Default \code{"SPL"} (study samples). Set
to \code{NA} to use all non-blank sample/QC types.}

\item{include_qualifier}{Include qualifier features. Default \code{FALSE}.}

\item{include_istd}{Include internal standards. Default \code{TRUE}.}

\item{include_feature_filter}{Feature(s) to include by \code{feature_id}, as a
character vector. Each element is matched exactly when it names an existing
feature, otherwise treated as a regex; elements combine with OR. A full ID
(e.g. \code{"S1P d18:0 [M>60]"}) needs no escaping, while patterns like \code{"PC|PE"}
still work. \code{NA} or \code{""} ignores the filter.}

\item{exclude_feature_filter}{Feature(s) to exclude by \code{feature_id}, matched
the same way as \code{include_feature_filter}. \code{NA} or \code{""} ignores the filter.}

\item{min_correction_pct}{Keep only features whose median correction (percent
of raw signal removed) is at least this value. \code{NA} (default) keeps every
feature with a positive impact. Use to drop negligible corrections.}

\item{binwidth}{Histogram bin width (percent). Default \code{NA} (30 bins).}

\item{font_base_size}{Base font size. Default \code{8}.}
}
\value{
A \code{ggplot} object: feature count vs. percent of signal removed.
}
\description{
Shows how many features were affected by which magnitude of
interference correction, as a histogram of the per-feature correction impact
(percent of raw signal removed) within one or more QC types (study samples by
default). The data must already be interference-corrected. Features with no
impact are excluded and reported.
}
\seealso{
Other QC plots:
\code{\link[=plot_feature_correlations]{plot_feature_correlations()}},
\code{\link[=plot_interference_correction]{plot_interference_correction()}},
\code{\link[=plot_matrixeffects]{plot_matrixeffects()}},
\code{\link[=plot_normalization_qc]{plot_normalization_qc()}},
\code{\link[=plot_pca]{plot_pca()}},
\code{\link[=plot_pca_loading]{plot_pca_loading()}},
\code{\link[=plot_qc_summary_byclass]{plot_qc_summary_byclass()}},
\code{\link[=plot_qc_summary_overall]{plot_qc_summary_overall()}},
\code{\link[=plot_qcmetrics_comparison]{plot_qcmetrics_comparison()}},
\code{\link[=plot_rla_boxplot]{plot_rla_boxplot()}},
\code{\link[=plot_rt_vs_chain]{plot_rt_vs_chain()}},
\code{\link[=plot_runscatter]{plot_runscatter()}},
\code{\link[=plot_runsequence]{plot_runsequence()}}
}
\concept{QC plots}
