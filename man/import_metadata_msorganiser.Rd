% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-import.R
\name{import_metadata_msorganiser}
\alias{import_metadata_msorganiser}
\title{Import metadata from a MRMhub Metadata Organizer file}
\usage{
import_metadata_msorganiser(
  data = NULL,
  path,
  ignore_warnings = FALSE,
  excl_unmatched_analyses = FALSE
)
}
\arguments{
\item{data}{A \code{MRMhubExperiment} object}

\item{path}{File name and path of the 'MRMhub Metadata Organizer' file (.xlsx) file}

\item{ignore_warnings}{Ignore warnings from data validation and proceed with importing metadata}

\item{excl_unmatched_analyses}{Exclude analyses (samples) that have no matching metadata}
}
\value{
An updated \code{MRMhubExperiment} object
}
\description{
Imports metadata from a 'MRMhub Metadata Organizer' file (.xlsx) file and associates it with analysis data.
}
\section{Identifier normalization}{

All imported identifiers are whitespace-normalized on import: leading and
trailing spaces are removed and internal runs of whitespace are collapsed to
a single space (for example \code{"QC  01"} becomes \code{"QC 01"}). Raw-data file
extensions (\code{.mzML}, \code{.d}, \code{.raw}, \code{.wiff}, \code{.wiff2}, \code{.lcd}, \code{.chrom},
case-insensitive) are stripped from \code{analysis_id}.

The same normalization is applied to both the data and the metadata, which is
what lets an \code{analysis_id} typed into metadata match the one derived from a
data-file name instead of silently failing to join. A consequence is that two
identifiers differing only by whitespace collapse to one and are then reported
as duplicates.
}

\examples{
mexp <- MRMhubExperiment()

mexp <- import_data_mrmhub(
  data = mexp,
  path = system.file("extdata", "MRMhub_demo.tsv", package = "mrmhub"),
  import_metadata = TRUE)

mexp <- import_metadata_msorganiser(
 data = mexp,
 path = system.file("extdata", "Example_Metadata_1.xlsx", package = "mrmhub"),
 excl_unmatched_analyses = FALSE,
 ignore_warnings = TRUE)

print(mexp)

}
