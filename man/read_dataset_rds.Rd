% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-export.R
\name{read_dataset_rds}
\alias{read_dataset_rds}
\title{Read a complete \code{MRMhubExperiment} from an RDS file}
\usage{
read_dataset_rds(path, verify = TRUE, show_status = FALSE)
}
\arguments{
\item{path}{A character string with the path to the \code{.rds} file.}

\item{verify}{A logical value. If \code{TRUE} (default) and the file carries an
embedded fingerprint, it is recomputed and compared; a mismatch triggers a
warning (the object is still returned).}

\item{show_status}{A logical value. If \code{TRUE}, the full processing and metadata
report (\code{\link[=mrmhub_status]{mrmhub_status()}}) is printed after loading. Default is \code{FALSE}, which
prints only the compact one-line overview.}
}
\value{
The loaded \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object.
}
\description{
Reads an \code{.rds} file written by \code{\link[=save_dataset_rds]{save_dataset_rds()}} (or a bare \code{saveRDS()}) back
into an \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}}, with feedback and a class
check. The \code{.rds} is a self-contained snapshot of a complete analysis project, so
the reloaded object can be inspected, re-plotted or re-processed with \pkg{mrmhub}
without the original input files or metadata.

If the file carries an embedded content fingerprint (see \code{\link[=save_dataset_rds]{save_dataset_rds()}}),
it is verified against the reloaded object: a match confirms the file holds the
object it was saved from, and the fingerprint is printed so it can be compared
with a recorded value.

This function only adds the class check, fingerprint verification and console
feedback on top of \code{\link[=readRDS]{readRDS()}}; the file itself is a plain R serialization.
It can therefore always be reopened with base R's \code{readRDS()} without
\pkg{mrmhub}, and every slot (the \code{dataset}, the \verb{annot_*} metadata tables, the
QC and calibration metrics) is stored as an ordinary tibble that can be
inspected via the \code{@} slots or \code{\link[=attributes]{attributes()}}. \pkg{mrmhub} is only required to
re-plot or re-process the object — which makes the \code{.rds} a convenient
self-contained archive of a complete analysis.
}
\examples{
mexp <- data_load_example()
path <- file.path(tempdir(), "example_mexp.rds")
save_dataset_rds(mexp, path)
mexp2 <- read_dataset_rds(path)

}
\seealso{
\code{\link[=save_dataset_rds]{save_dataset_rds()}}, \code{\link[=mrmhub_status]{mrmhub_status()}}
}
