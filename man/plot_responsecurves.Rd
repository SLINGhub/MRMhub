% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-qc-responsecurves.R
\name{plot_responsecurves}
\alias{plot_responsecurves}
\title{Plot response curves}
\usage{
plot_responsecurves(
  data = NULL,
  variable = "intensity",
  filter_data = FALSE,
  include_qualifier = TRUE,
  include_istd = TRUE,
  include_feature_filter = NA,
  exclude_feature_filter = NA,
  max_regression_value = NA,
  output_pdf = FALSE,
  path = NA,
  return_plots = FALSE,
  color_curves = NULL,
  point_size = NULL,
  line_width = 0.7,
  label_wrap = FALSE,
  label_wrap_width = 25,
  font_base_size = NULL,
  autoscale = TRUE,
  legend_position = "right",
  legend_size = NULL,
  rows_page = 4,
  cols_page = 5,
  curve_layout = "overlay",
  fixed_scale_curves = FALSE,
  r2_vstep = 0.06,
  specific_page = NA,
  page_orientation = "LANDSCAPE",
  show_progress = TRUE
)
}
\arguments{
\item{data}{A \code{MRMhubExperiment} object.}

\item{variable}{The variable to plot on the y-axis.}

\item{filter_data}{Whether to use all data (default) or only
QC-filtered data (filtered via \code{\link[=filter_features_qc]{filter_features_qc()}}).}

\item{include_qualifier}{Logical, whether to include qualifier features. Default is \code{TRUE}.}

\item{include_istd}{Logical, whether to include internal standard (ISTD) features. Default is \code{TRUE}.}

\item{include_feature_filter}{Feature(s) to include by \code{feature_id}, as a
character vector. Each element is matched exactly when it names an existing
feature, otherwise treated as a regex; elements combine with OR. A full ID
(e.g. \code{"S1P d18:0 [M>60]"}) needs no escaping, while patterns like \code{"PC|PE"}
still work. \code{NA} or \code{""} ignores the filter.}

\item{exclude_feature_filter}{Feature(s) to exclude by \code{feature_id}, matched
the same way as \code{include_feature_filter}. \code{NA} or \code{""} ignores the filter.}

\item{max_regression_value}{The maximum sample_amount (x) value for fitting
the regression line. If \code{NA}, regression is based on all data points.}

\item{output_pdf}{If \code{TRUE}, saves the generated plots as a PDF
file. When \code{FALSE}, plots are directly plotted.}

\item{path}{The file path for saving the PDF. Must be defined if
\code{output_pdf} is \code{TRUE}.}

\item{return_plots}{Logical. If \code{TRUE}, returns the plots as a list of
\code{ggplot} objects.}

\item{color_curves}{A vector of colors for the curves. If \code{NULL} (default),
the colors for each curve are generated automatically. If colors are provided,
the number of colors must match the number of curves.}

\item{point_size}{Size of points in millimeters.}

\item{line_width}{Width of regression lines.}

\item{label_wrap}{Logical. If \code{TRUE}, long \code{feature_id} labels are wrapped to multiple
lines using \code{label_wrap_width}. Default is \code{FALSE}.}

\item{label_wrap_width}{Integer. Maximum width in characters for wrapped labels when
\code{label_wrap = TRUE}. Default is \code{25}. Ignored when \code{label_wrap = FALSE}.}

\item{font_base_size}{Numeric. Base font size (in points) for plot text; all
plot text scales proportionally with this value. See the function's default
in the Usage section above.}

\item{autoscale}{Logical. When \code{TRUE} (default), \code{font_base_size} and
\code{point_size} left as \code{NULL} are sized automatically from \code{cols_page} (more
facet columns per page give smaller text and points). Any value passed
explicitly always takes precedence. When \code{FALSE}, unset sizes fall back to
the single-plot defaults.}

\item{legend_position}{Optional legend placement. One of \code{"right"},
\code{"left"}, \code{"top"}, \code{"bottom"}, \code{"none"}; a corner keyword \code{"inside-tr"},
\code{"inside-tl"}, \code{"inside-br"}, \code{"inside-bl"}; or a numeric \code{c(x, y)} in
\verb{[0, 1]} coordinates. \code{NULL} (default) keeps the current placement.}

\item{legend_size}{Optional single multiplier of \code{font_base_size} (when
\verb{<= 3}) or absolute point size (when \verb{> 3}) that scales the whole legend:
text, title, key and the plotted symbols. \code{NULL} (default) leaves the
legend unchanged.}

\item{rows_page}{Number of rows of plots per page. Used for pagination in
\code{curve_layout = "overlay"} and \code{"cols"}. Ignored in \code{curve_layout = "rows"}.}

\item{cols_page}{Number of columns of plots per page. Used for pagination in
\code{curve_layout = "overlay"} and \code{"rows"}. Ignored in \code{curve_layout = "cols"}.}

\item{curve_layout}{Controls how multiple curves are displayed. One of:
\describe{
\item{\code{"overlay"}}{(default) All curves overlaid in each feature panel using
\code{facet_wrap2}. Pagination uses \code{rows_page * cols_page}.}
\item{\code{"cols"}}{Grid layout with features as rows and curves as columns
using \code{facet_grid2}. Pagination uses \code{rows_page} only.}
\item{\code{"rows"}}{Grid layout with curves as rows and features as columns
using \code{facet_grid2}. Pagination uses \code{cols_page} only.}
}}

\item{fixed_scale_curves}{Logical. If \code{TRUE}, fixes the y-axis scale per feature row
(\code{curve_layout = "cols"}) or per curve row (\code{curve_layout = "rows"}). If \code{FALSE}
(default), each panel auto-scales. Silently ignored when \code{curve_layout = "overlay"}.}

\item{r2_vstep}{Numeric. Vertical step between stacked R-squared labels when multiple
curves are plotted in the same panel (\code{curve_layout = "overlay"}). Default is \code{0.06}.
Ignored when \code{curve_layout} is \code{"cols"} or \code{"rows"}, where each panel has one curve.}

\item{specific_page}{An integer specifying a specific page to plot. If
\code{NA} (default), all pages are plotted.}

\item{page_orientation}{Orientation of the PDF paper: \code{"LANDSCAPE"} or
\code{"PORTRAIT"}.}

\item{show_progress}{Logical. If \code{TRUE}, displays a progress bar during
plot creation.}
}
\value{
If \code{return_plots} is \code{TRUE}, a list of \code{ggplot} objects is
returned. Otherwise, the function saves the plot output or does not return
anything.
}
\description{
This function plots response curves for each feature. Multiple response curves,
each with a linear regression line, can be plotted on the same graph.
Each feature is displayed as a separate facet.
}
\details{
Features for plotting can be filtered using QC filters defined via
\code{\link[=filter_features_qc]{filter_features_qc()}} or through \code{include_feature_filter} and
\code{exclude_feature_filter} arguments. The resulting plots offer extensive
customization options, including point size, line width, point color, point
fill, point shape, line color, ribbon fill, and font base size.

Plots will be divided into multiple pages if the number of features exceeds
the product of \code{rows_page} and \code{cols_page} settings. The function supports
both direct plotting within R and saving plots as PDF files. Additionally,
plots can be returned as a list of ggplot2 objects for further manipulation
or integration into other analyses.
}
\seealso{
Other calibration plots:
\code{\link[=plot_calibrationcurves]{plot_calibrationcurves()}}
}
\concept{calibration plots}
