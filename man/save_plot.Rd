% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-save.R
\name{save_plot}
\alias{save_plot}
\title{Save a plot to a file}
\usage{
save_plot(
  plot,
  path,
  width,
  height,
  units = NULL,
  dpi = NULL,
  format = NULL,
  scale = 1,
  bg = NULL,
  create_dir = TRUE,
  overwrite = TRUE,
  show_plot = TRUE,
  ...
)
}
\arguments{
\item{plot}{The plot to save. A \code{ggplot} object (what most \verb{plot_*()}
functions return), a \code{patchwork} composition, a list carrying the plot in a
\code{plot} element (as \code{\link[=plot_rla_boxplot]{plot_rla_boxplot()}} returns), or a list of \code{ggplot}
objects, which is written as a multi-page PDF. Lists of plots are returned
by the paged plot functions with \code{return_plots = TRUE}.}

\item{path}{Output file path. The extension selects the format unless
\code{format} is given, in which case a known extension is replaced.}

\item{width, height}{Figure size, in \code{units}. Both are required: the physical
size of a saved figure is always explicit at the call site, so it can be
read off the code rather than inherited from a session setting.}

\item{units}{Unit of \code{width} and \code{height}: \code{"mm"} (default), \code{"cm"}, \code{"in"},
\code{"pt"} or \code{"px"}. \code{NULL} uses the global default set by
\code{\link[=mrmhub_set_plot_defaults]{mrmhub_set_plot_defaults()}} if one is in effect, otherwise \code{"mm"}.}

\item{dpi}{Resolution in dots per inch for the raster formats (\code{png}, \code{tiff},
\code{jpeg}), and the reference resolution when \code{units = "px"}. Ignored for the
vector formats. \code{NULL} uses the global default set by
\code{\link[=mrmhub_set_plot_defaults]{mrmhub_set_plot_defaults()}} if one is in effect, otherwise \code{300}.}

\item{format}{Output format(s): one or more of \code{"pdf"}, \code{"svg"}, \code{"png"},
\code{"tiff"}, \code{"jpeg"}. \code{NULL} (default) takes the format from the extension of
\code{path}. Several formats write one file each, sharing the same base name.}

\item{scale}{Multiplicative scaling factor applied to the plot, as in
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. Values \verb{> 1} make text and symbols smaller relative to
the figure.}

\item{bg}{Background colour. \code{NULL} (default) uses the plot's own background.}

\item{create_dir}{A logical value. If \code{TRUE} (the default), the parent
directory of \code{path} is created if it does not yet exist.}

\item{overwrite}{A logical value indicating whether existing files may be
overwritten. Default is \code{TRUE}.}

\item{show_plot}{A logical value. If \code{TRUE} (the default), the plot is
returned \emph{visibly}, so that piping into \code{save_plot()} still renders the
figure in a Quarto or R Markdown chunk and the call reads as one statement.
The written paths are then attached as a \code{"paths"} attribute. If \code{FALSE},
nothing is drawn and the paths are returned invisibly, which is preferable
in scripts and loops where re-drawing a dense figure is wasted work.}

\item{...}{Further arguments passed to the graphics device.}
}
\value{
If \code{show_plot = TRUE}, the plot itself, visibly, with the written
paths in its \code{"paths"} attribute. If \code{show_plot = FALSE}, invisibly, a
character vector of the paths written. For multi-page output the list of
plots is returned in place of a single plot.
}
\description{
Writes a plot created by any of the \verb{plot_*()} functions to a file at a
defined physical size and resolution, so figures do not have to be exported
with hand-written \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} calls. Dimensions can be given in
millimetres, centimetres, inches, points or pixels, and the same figure can be
written in several formats in one call.
}
\section{Preferred formats and devices}{
\tabular{llll}{
   Purpose \tab Format \tab Device used \tab Typical \code{dpi} \cr
   Journal figure, vector (default choice) \tab \code{"pdf"} \tab \code{grDevices::cairo_pdf}, else \code{grDevices::pdf} \tab n/a \cr
   Figure for further editing (Illustrator, Inkscape) \tab \code{"svg"} \tab \code{svglite::svglite}, else \code{grDevices::svg} \tab n/a \cr
   Slides, Quarto HTML, GitHub \tab \code{"png"} \tab \code{ragg::agg_png}, else \code{grDevices::png} \tab 150-300 \cr
   Journal requiring raster submission \tab \code{"tiff"} \tab \code{ragg::agg_tiff}, else \code{grDevices::tiff} \tab 300-600 \cr
}


Prefer a \strong{vector} format (\code{pdf}, \code{svg}) for publication: text stays
selectable and searchable, and lines stay sharp at any magnification.

Prefer a \strong{raster} format (\code{png}, \code{tiff}) when a plot draws very many marks
-- a \code{\link[=plot_runscatter]{plot_runscatter()}} page covering several thousand analyses, or a dense
\code{\link[=plot_pca]{plot_pca()}} score plot. Every point becomes a separate object in a PDF, so
such figures produce very large files that are slow to open and to typeset.
Saving them at 300-600 dpi instead keeps the file small with no visible loss.

The optional packages \code{ragg} and \code{svglite} are used automatically when
installed, giving better text rendering, system-font support and smaller SVG
files. When they are absent the equivalent \code{grDevices} device is used and the
output is still correct. Installing both is recommended:
\code{install.packages(c("ragg", "svglite"))}.

PDF output uses the cairo device wherever R was built with cairo support
(\code{capabilities("cairo")}), because plain \code{grDevices::pdf()} writes text in a
single-byte encoding and silently transliterates anything outside it -- an en
dash becomes \code{-}, \code{>=} replaces the proper symbol. Unit labels such as
\code{umol/L} and statistical annotations routinely depend on those glyphs.

Multi-page output from the paged plot functions (\code{\link[=plot_runscatter]{plot_runscatter()}},
\code{\link[=plot_calibrationcurves]{plot_calibrationcurves()}}, \code{\link[=plot_responsecurves]{plot_responsecurves()}},
\code{\link[=plot_feature_correlations]{plot_feature_correlations()}}) is PDF only, which is the only format that
holds many pages in one file. Use \code{\link[=save_plot]{save_plot()}} for single figures in any of
the other formats.
}

\examples{
\dontrun{
p <- plot_pca(mexp, variable = "norm_intensity", qc_types = c("BQC", "SPL"))

# A single figure, sized in mm (the default unit)
save_plot(p, "output/pca.pdf", width = 180, height = 120)

# In a notebook: save and show the figure in one statement
plot_pca(mexp, variable = "norm_intensity") |>
  save_plot("output/pca.pdf", width = 180, height = 120)

# The same figure as vector and raster in one call
save_plot(p, "output/pca", format = c("pdf", "png"), width = 180, height = 120)

# In a script or loop, skip the re-draw and collect the paths
paths <- save_plot(p, "output/pca.pdf", width = 180, height = 120,
                   show_plot = FALSE)

# Every runscatter page in one multi-page PDF
pages <- plot_runscatter(mexp, variable = "conc", return_plots = TRUE)
save_plot(pages, "output/runscatter.pdf", width = 280, height = 200,
          show_plot = FALSE)
}

}
\seealso{
\code{\link[=mrmhub_set_plot_defaults]{mrmhub_set_plot_defaults()}} to set \code{units} and \code{dpi} once for a
whole notebook, \code{\link[=plot_runscatter]{plot_runscatter()}} and the other paged plot functions for
multi-page PDF output, and \code{\link[=save_report_xlsx]{save_report_xlsx()}} to export the data.
}
