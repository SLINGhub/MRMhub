% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-qc-interference.R
\name{plot_interference_correction}
\alias{plot_interference_correction}
\title{Plot the results of interference correction}
\usage{
plot_interference_correction(
  data,
  qc_types = c("SPL", "TQC", "PBLK", "BQC"),
  include_qualifier = FALSE,
  include_istd = TRUE,
  include_feature_filter = NA,
  exclude_feature_filter = NA,
  min_median_value = NA,
  min_correction_pct = NA,
  sort_by_effect = c("none", "desc", "asc"),
  top_n = NA,
  y_lim = c(-NA, NA),
  point_size = 0.5,
  dodge_width = 0.6,
  point_alpha = 0.3,
  box_alpha = 0.3,
  box_linewidth = 0.5,
  font_base_size = 8,
  angle_x = 45
)
}
\arguments{
\item{data}{A \code{MRMhubExperiment} object.}

\item{qc_types}{A character vector specifying the QC types to plot. It must
contain at least one element. The default \code{NA} plots any of the non-blank
QC types ("SPL", "TQC", "BQC", "HQC", "MQC", "LQC", "NIST", "LTR") present
in the dataset.}

\item{include_qualifier}{A logical value indicating whether to include
qualifier features. Default is \code{TRUE}.}

\item{include_istd}{A logical value indicating whether to include internal
standards (ISTD) features.  Default is \code{TRUE}.}

\item{include_feature_filter}{Feature(s) to include by \code{feature_id}, as a
character vector. Each element is matched exactly when it names an existing
feature, otherwise treated as a regex; elements combine with OR. A full ID
(e.g. \code{"S1P d18:0 [M>60]"}) needs no escaping, while patterns like \code{"PC|PE"}
still work. \code{NA} or \code{""} ignores the filter.}

\item{exclude_feature_filter}{Feature(s) to exclude by \code{feature_id}, matched
the same way as \code{include_feature_filter}. \code{NA} or \code{""} ignores the filter.}

\item{min_median_value}{Median raw-signal abundance floor: drop features whose
median \code{feature_intensity} across the selected QC types is below this value.
\code{NA} (default) applies no threshold. Use to hide low-signal features.}

\item{min_correction_pct}{Keep only features whose median correction (percent
of raw signal removed, across the selected QC types) is at least this value.
\code{NA} (default) applies no threshold. Use to focus the plot on
substantially-corrected features.}

\item{sort_by_effect}{Order the x-axis by correction effect, defined per
feature as the deviation of its median \% change from 100\% (pooled across the
displayed QC-type points). One of \code{"none"} (default, alphabetical),
\code{"desc"} (largest effect first) or \code{"asc"}.}

\item{top_n}{Keep only the \code{top_n} features with the largest correction
effect (see \code{sort_by_effect}). \code{NA} (default) keeps all. Applied after the
\code{min_median_value} / \code{min_correction_pct} filters.}

\item{y_lim}{A numeric vector of length 2 specifying the y-axis limits.}

\item{point_size}{A numeric value indicating the size of points in
millimeters. Default is \code{0.5}.}

\item{dodge_width}{Numeric. Width used to dodge overlapping points by \code{qc_type}. Default is \code{0.6}.}

\item{point_alpha}{Numeric. Transparency of the plotted points. Default is \code{0.3}.}

\item{box_alpha}{Numeric. Transparency of the boxplot. Default is \code{0.3}.}

\item{box_linewidth}{Numeric. Width of the boxplot lines. Default is \code{0.5}.}

\item{font_base_size}{Numeric. Base font size (in points) for plot text.
Default is 8.}

\item{angle_x}{Numeric. Angle of the x-axis text labels. Default is \code{45}.}
}
\value{
A \code{ggplot} object showing the grouped standardized beeswarm plot.
}
\description{
This function generates grouped standardized beeswarm plots to visualize the results of interference correction
across different QC types.
}
\seealso{
Other QC plots:
\code{\link[=plot_feature_correlations]{plot_feature_correlations()}},
\code{\link[=plot_matrixeffects]{plot_matrixeffects()}},
\code{\link[=plot_normalization_qc]{plot_normalization_qc()}},
\code{\link[=plot_pca]{plot_pca()}},
\code{\link[=plot_pca_loading]{plot_pca_loading()}},
\code{\link[=plot_qc_interference_impact]{plot_qc_interference_impact()}},
\code{\link[=plot_qc_summary_byclass]{plot_qc_summary_byclass()}},
\code{\link[=plot_qc_summary_overall]{plot_qc_summary_overall()}},
\code{\link[=plot_qcmetrics_comparison]{plot_qcmetrics_comparison()}},
\code{\link[=plot_rla_boxplot]{plot_rla_boxplot()}},
\code{\link[=plot_rt_vs_chain]{plot_rt_vs_chain()}},
\code{\link[=plot_runscatter]{plot_runscatter()}},
\code{\link[=plot_runsequence]{plot_runsequence()}}
}
\concept{QC plots}
