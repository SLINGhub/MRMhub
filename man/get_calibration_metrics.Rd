% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-calibrations.R
\name{get_calibration_metrics}
\alias{get_calibration_metrics}
\title{Get calibration metrics}
\usage{
get_calibration_metrics(
  data = NULL,
  include_qualifier = TRUE,
  with_lod = TRUE,
  with_loq = TRUE,
  with_coefficients = TRUE,
  with_sigma = TRUE,
  summary_table = FALSE
)
}
\arguments{
\item{data}{A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object with QC metrics.}

\item{include_qualifier}{Whether to include qualifier features. When \code{FALSE},
only quantifier features are returned. Default is \code{TRUE}.}

\item{with_lod}{Whether to include LoD in output. Default is \code{TRUE}.}

\item{with_loq}{Whether to include LoQ in output. Default is \code{TRUE}.}

\item{with_coefficients}{Whether to include regression coefficients. Default is \code{TRUE}.}

\item{with_sigma}{Whether to include sigma in output. Default is \code{TRUE}.}

\item{summary_table}{When \code{TRUE}, return a compact, display-formatted summary
tibble (\code{analyte}, \code{fit_model}, \code{fit_weighting}, \code{r2}, \code{lod}, \code{loq}) with
\code{r2} rounded to 5 decimals and \code{lod}/\code{loq} to 3 significant figures. This is
a presentation view for reporting; the values are \strong{rounded}, so do not use
it for downstream computation. \code{lod}/\code{loq} follow \code{with_lod}/\code{with_loq}. The
\code{with_coefficients}/\code{with_sigma} flags are ignored in this mode. Default is
\code{FALSE} (full, unrounded metrics).}
}
\value{
A tibble with exported calibration metrics.
}
\description{
Extracts calibration fit metrics from a \code{MRMhubExperiment} object.
}
\details{
Requires prior computation of regression results using \code{\link[=calc_calibration_results]{calc_calibration_results()}}.
See its documentation for details.
\subsection{Returned Details and Metrics}{
\itemize{
\item \code{feature_id}: Feature identifier.
\item \code{is_quantifier}: Logical, indicates if the feature is a quantifier.
\item \code{fit_model}: Regression model used for fitting.
\item \code{fit_weighting}: Weighting method used in fitting.
\item \code{lowest_cal}: Lowest nonzero calibration concentration.
\item \code{highest_cal}: Highest calibration concentration.
\item \code{r2}: R² value, indicating goodness of fit. For a \strong{weighted}
fit this is the weighted coefficient of determination (computed from weighted
sums of squares), matching the value reported by vendor software such as
Agilent MassHunter for the same weighted curve.
\item \code{coef_a}: \strong{Intercept} (0th-order term, \code{x^0}) of the fitted curve.
\item \code{coef_b}: \strong{1st-order} (linear, \code{x^1}) coefficient — the slope in a \strong{linear} fit, or the linear term in a \strong{quadratic} fit.
\item \code{coef_c}: \strong{2nd-order} (quadratic, \code{x^2}) coefficient in \strong{quadratic} fits. Returns \code{NA} for \strong{linear} fits.
\item \code{sigma}: Standard deviation of residuals.
\item \code{reg_failed}: \code{TRUE} if regression fitting failed.
\item \code{LoD} = 3.3× the sample standard error of residuals / slope of the regression (see Notes).
\item \code{LoQ} = 10× the sample standard error of residuals / slope of the regression (see Notes).
}

The coefficients describe the curve in \strong{ascending} power order (R's
\code{lm()} / \code{poly()} convention): \code{response = coef_a + coef_b * x + coef_c * x^2}.
This is the reverse of the descending \code{a*x^2 + b*x + c} form used by some
vendor software (e.g. Agilent MassHunter, where \code{a} is the \code{x^2} term), so
when comparing against such an export, match by power, not by letter.

\strong{Note:} LoD/LoQ follow the ICH Q2(R1/R2) approach (3.3 sigma / S and
10 sigma / S). The slope \code{S} is the slope of the calibration curve at zero
concentration (the linear coefficient \code{coef_b}); for a \strong{quadratic} fit the
quadratic term does not contribute to this slope. The response \code{sigma} is
selectable in \code{\link[=calc_calibration_results]{calc_calibration_results()}} via \code{lod_sigma} (residual
standard error, the default, or the standard error of the intercept); the
\code{sigma} column reported here is always the residual standard error.

For a \strong{weighted} fit (\code{1/x}, \code{1/x^2}, \code{1/sqrt(x)}) \code{sigma} is R's weighted
residual standard error, which is not on the raw response scale that the ICH
\verb{3.3 sigma / S} formula assumes, so the reported LoD/LoQ are approximate
(typically slightly optimistic for \code{1/x}). Use \code{fit_weighting = "none"} if you
require the strict ICH response-scale \code{Sy/x}; the back-calculated
concentrations themselves are unaffected.
}
}
