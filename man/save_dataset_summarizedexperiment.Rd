% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-summarizedexperiment.R
\name{save_dataset_summarizedexperiment}
\alias{save_dataset_summarizedexperiment}
\title{Export an experiment to a Bioconductor SummarizedExperiment}
\usage{
save_dataset_summarizedexperiment(
  data = NULL,
  path = NULL,
  variable = NULL,
  as = c("SummarizedExperiment", "LipidomicsExperiment"),
  filter_data = FALSE,
  overwrite = TRUE
)
}
\arguments{
\item{data}{An \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object.}

\item{path}{Optional file path. When given, the object is written there with
\code{\link[=saveRDS]{saveRDS()}} and returned invisibly; a \code{.rds} extension is appended if
missing. When \code{NULL} (default) the object is returned.}

\item{variable}{Feature variables to export as assays, e.g. \code{"conc"} or
\code{c("intensity", "conc")}. \code{NULL} (default) exports every \verb{feature_*}
variable present in the data.}

\item{as}{Class to produce. \code{"SummarizedExperiment"} (default) or
\code{"LipidomicsExperiment"}, which additionally requires the \code{lipidr} package
and is only meaningful for lipidomics data.}

\item{filter_data}{Use QC-filtered data (\code{dataset_filtered}, see
\code{\link[=filter_features_qc]{filter_features_qc()}}) instead of the full dataset. Default \code{FALSE}.}

\item{overwrite}{Overwrite an existing file at \code{path}. Default \code{TRUE}.}
}
\value{
A \code{SummarizedExperiment} (or \code{LipidomicsExperiment}). Returned
invisibly when \code{path} is given.
}
\description{
Converts an \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} to a
\href{https://bioconductor.org/packages/SummarizedExperiment/}{SummarizedExperiment},
the Bioconductor container for feature x sample data, and optionally to a
\code{LipidomicsExperiment} for use with
\href{https://www.lipidr.org}{lipidr}. This opens the experiment to the
Bioconductor ecosystem - \code{limma} for differential abundance, \code{POMA} and \code{pmp}
for preprocessing.
}
\details{
\strong{Layout.} Features are rows and analyses are columns, following the
SummarizedExperiment convention. Each feature variable becomes one assay, so
\code{feature_intensity}, \code{feature_norm_intensity} and \code{feature_conc} sit
side-by-side in the same object and are addressed with
\code{SummarizedExperiment::assay(se, "conc")}. Assay names drop the \code{feature_}
prefix. \code{annot_features} becomes \code{rowData()}, \code{annot_analyses} becomes
\code{colData()}, and the processing state (status, flags, concentration unit)
becomes \code{metadata()}.

\strong{Everything is exported.} Internal standards, QC samples, blanks and
calibrants are all included and flagged rather than dropped, because
downstream tools need them: \code{lipidr} requires the \code{istd} annotation and
\code{pmp}'s blank filter needs blanks present. Subset when you need to:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{se[!rowData(se)$is_istd, se$qc_type == "SPL"]
}\if{html}{\out{</div>}}

Most statistical tools will otherwise happily include blanks and calibrants
and return nonsense.

\strong{QC metrics} are not written to \code{rowData()}. To filter features by QC
criteria, use \code{\link[=filter_features_qc]{filter_features_qc()}} and export with \code{filter_data = TRUE}.
\code{\link[=save_feature_qc_metrics]{save_feature_qc_metrics()}} exports the metrics themselves.
}
\examples{
\dontshow{if (rlang::is_installed("SummarizedExperiment")) withAutoprint(\{ # examplesIf}
mexp <- normalize_by_istd(lipidomics_dataset)
mexp <- quantify_by_istd(mexp)

se <- save_dataset_summarizedexperiment(mexp)
SummarizedExperiment::assayNames(se)

# study samples only, internal standards dropped
se[!SummarizedExperiment::rowData(se)$is_istd, se$qc_type == "SPL"]
\dontshow{\}) # examplesIf}
}
\references{
Morgan M, Obenchain V, Hester J, & Pagès H (2026). SummarizedExperiment: A
container (S4 class) for matrix-like assays. R package version 1.42.0.
\doi{10.18129/B9.bioc.SummarizedExperiment}
\url{https://bioconductor.org/packages/SummarizedExperiment}

Mohamed A, Molendijk J, & Hill MM (2020). lipidr: A Software Tool for Data
Mining and Analysis of Lipidomics Datasets. \emph{Journal of Proteome Research},
19(7), 2890-2897. \doi{10.1021/acs.jproteome.0c00082}
}
\seealso{
\code{\link[=save_dataset_csv]{save_dataset_csv()}}, \code{\link[=save_dataset_mztab]{save_dataset_mztab()}}, \code{\link[=save_report_xlsx]{save_report_xlsx()}}
}
