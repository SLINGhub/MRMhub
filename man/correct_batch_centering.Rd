% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct-drift-batch.R
\name{correct_batch_centering}
\alias{correct_batch_centering}
\title{Batch centering correction}
\usage{
correct_batch_centering(
  data = NULL,
  variable,
  ref_qc_types,
  correct_scale = FALSE,
  replace_previous = TRUE,
  log_transform_internal = TRUE,
  feature_list = NULL,
  replace_exisiting_trendcurves = FALSE,
  ...
)
}
\arguments{
\item{data}{A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object containing the data to be corrected.
This object must include information about QC types and measurements.}

\item{variable}{The variable to be corrected. Must be one of "intensity",
"norm_intensity", or "conc".}

\item{ref_qc_types}{A character vector specifying the QC types to be
used as references for batch centering.}

\item{correct_scale}{A logical value indicating whether to equalize the scale
of the batches in addition to center them. Defaults to \code{FALSE}.}

\item{replace_previous}{A logical value indicating whether to replace any
previous batch corrections or apply the new correction on top. Defaults to
\code{TRUE} (replace).}

\item{log_transform_internal}{A logical value indicating whether to log-transform
the data internally during correction. Defaults to \code{TRUE}. This also sets the
centering model: with \code{TRUE} the batch reference levels are aligned in log
space, i.e. \strong{multiplicative (geometric)} centering (the appropriate choice
for multiplicatively-scaling MS intensities, and the reason it is the
default); with \code{FALSE} they are aligned in raw space, i.e. \strong{additive}
centering (which can shift low values below zero). Either way the returned
data are on the raw (untransformed) scale.}

\item{feature_list}{Sets specific features for correction only. Can be character vector or a single string which is then interpreted as regular expression. Default is \code{NULL} which means all features are selected.}

\item{replace_exisiting_trendcurves}{A logical value indicating whether to replace
trend curves from previous corrections. This is only use for plotting using \code{plot_runscatter()}. Default is \code{FALSE}.}

\item{...}{Additional arguments that can be passed to the batch correction
function.}
}
\value{
A \code{\link[=MRMhubExperiment-class]{MRMhubExperiment}} object containing the corrected data.
}
\description{
This function performs batch centering correction on each feature.
Optionally, the scale of the batches can be equalized across batches.
The selected QC types (\code{ref_qc_types}) are used to calculate
the medians, which are then used to align all other samples. The
correction can be applied to one of three variables: "intensity",
"norm_intensity", or "conc". The correction can either be applied
on top of previous corrections or replace all prior batch corrections.
}
\seealso{
\code{\link[=plot_runscatter]{plot_runscatter()}} for visualizing the correction before and after; the \href{https://slinghub.github.io/MRMhub/quant/articles/tutorial-04-drift-correction.html}{drift-correction tutorial} for a worked example.
}
