% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-mztab.R
\name{import_data_mztab}
\alias{import_data_mztab}
\title{Import data from an mzTab-M file}
\usage{
import_data_mztab(data = NULL, path, import_metadata = TRUE, silent = FALSE)
}
\arguments{
\item{data}{An \code{MRMhubExperiment} object (e.g. from \code{\link[=MRMhubExperiment]{MRMhubExperiment()}}).}

\item{path}{Path to a \code{.mzTab} file, or a directory of them.}

\item{import_metadata}{If \code{TRUE} (default), derive analysis/feature metadata
(incl. \code{batch_id}, formula, neutral mass) from the imported data via
\code{\link[=import_metadata_from_data]{import_metadata_from_data()}}.}

\item{silent}{Suppress messages.}
}
\value{
The updated \code{MRMhubExperiment}.
}
\description{
Imports quantitative results from an
\href{https://github.com/HUPO-PSI/mzTab-M}{mzTab-M} file (e.g. produced by
Lipid Data Analyzer, MS-DIAL or MZmine) into an \code{MRMhubExperiment}. Each
Small Molecule Feature (\code{SMF}) becomes an mrmhub feature and each assay an
analysis; the per-assay abundances are imported as \code{feature_intensity}.
}
\details{
mzTab-M is a quantification \emph{report}, so an import is necessarily partial:
the single reported abundance per feature is mapped to \code{feature_intensity},
and feature identities (name, formula, neutral mass, m/z, retention time)
are taken from the \code{SMF}/\code{SML} sections where available. Internal-standard
relationships, QC-type assignments and calibration metadata are \strong{not}
part of mzTab-M and must be supplied afterwards with \code{\link[=add_metadata]{add_metadata()}}.
\code{study_variable} group membership is imported best-effort as \code{batch_id}
(mzTab-M has no analytical-batch concept).
}
\examples{
\dontrun{
mexp <- MRMhubExperiment()
mexp <- import_data_mztab(mexp, "LDA_export.mzTab")
}

}
\seealso{
\code{\link[=import_data_mrmhub]{import_data_mrmhub()}}, \code{\link[=save_dataset_mztab]{save_dataset_mztab()}}, \code{\link[=add_metadata]{add_metadata()}}
}
